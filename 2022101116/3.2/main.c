#include "functions.h"

int main()
{
    int T;
    scanf("%d", &T);

    for (int i = 0; i < T; i++)
    {
        int N;
        for (int j = 0; j < N; j++)
        {
            int elem;
            scanf("%d ", &elem);
        }

        for (int j = 0; j < N; j++)
        {
            int elem;
            scanf("%d ", &elem);
        }
        
    }
    
}